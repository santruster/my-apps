import { Basket } from 'react-bootstrap-icons';
function CartWidget() {
    return (
      <div className="CartWidget">
       <Basket size={30}/>
      </div>
    );
  }
  
  export default CartWidget;